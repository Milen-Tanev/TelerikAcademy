namespace RotatingWalkInMatrix
{
    class Messages
    {
        internal const string AskUserForMatrixSize = "Enter a positive number";

        // Errors - not worth it creating new class for a single message
        internal const string InputIsNotAPositiveNumberError = "You haven't entered a positive number!";
        internal const string InputIsOutOfRangeError = "The input number must be between 0 and 100!";
    }
}
